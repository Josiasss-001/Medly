<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MEDLY_PROYECTO" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores base -->
        <item name="colorPrimary">@color/Celeste</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="android:windowBackground">#F8F9FA</item>

        <!-- ELIMINA EL MORADO DE LA BARRA INFERIOR (Píldora de selección) -->
        <item name="colorSecondaryContainer">@android:color/transparent</item>
        <item name="colorOnSecondaryContainer">@color/Celeste</item>

        <!-- Ripple effect -->
        <item name="colorControlHighlight">#15000000</item>
    </style>

    <style name="Theme.MEDLY_PROYECTO" parent="Base.Theme.MEDLY_PROYECTO" />

    <style name="CircleImage">
        <item name="cornerSize">50%</item>
    </style>

    <style name="RoundedImage">
        <item name="cornerSize">20dp</item>
    </style>

    <style name="RoundedTextInputLayout">
        <item name="cornerSize">16dp</item>
    </style>

    <style name="BottomRoundedCard">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">0dp</item>
        <item name="cornerSizeTopRight">0dp</item>
        <item name="cornerSizeBottomLeft">20dp</item>
        <item name="cornerSizeBottomRight">20dp</item>
    </style>

    <style name="CalendarDayHeader">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#999999</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>